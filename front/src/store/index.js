import { createStore } from 'vuex'
import tank from './tank'
import tankUser from './tankUser'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    tank, tankUser
  }
})
